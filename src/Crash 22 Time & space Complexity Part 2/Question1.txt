ans - 
    outer loop = O(n)
    inner loop = O(sqrt(n))
    so O(n* sqrt(n))
Calculate the time complexity for the following code snippet.
for(int i = 0; i < n; i++) {
    for(int j = 0; j * j < n; j++) {
        cout << “Crash 22 ”;
    }
}
