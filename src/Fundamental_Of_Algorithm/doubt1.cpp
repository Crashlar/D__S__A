#include <iostream>
using namespace std;

// (power in society)
// int > char 
// int > float  

int main(){
    cout << (0 ? 3 : '1');
    cout << endl;
    cout << (-4 > 0 ? 'p': 'N');
    return 0;
    
}

// doubt 

    // cout << (0 ? 3 : '1');
    // cout << (-4 > 0 ? 'p': 'N');